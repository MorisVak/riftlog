{
  "name": "riftlog",
  "version": "0.0.0",
  "private": true,
  "description": "Riftlog — a companion app for the Riftbound TCG.",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "mobile": "pnpm --filter @riftlog/mobile",
    "core": "pnpm --filter @riftlog/core",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rm -rf node_modules dist .expo .turbo && rm -rf node_modules"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "prettier": "^3.3.3"
  }
}
